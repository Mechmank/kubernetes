apiVersion: v1
kind: pod
metadata:
  name: redis
  labels:
    app: redis
spec:
  containers:
    - name: redis
      image: redis:alpine
      ports:
        - containerPort: 6379            # Default listening port for redis: 6379
          name: redis
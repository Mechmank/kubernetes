# Storage class is used to create a PV by the PVC as desired specifications
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
   name: my-storageclass
provisioner: k8s.io/minikube-hostpth   #Path to create a PV
reclaimPolicy: Delete  

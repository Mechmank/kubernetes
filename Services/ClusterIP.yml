---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 80       # Service port
      targetPort: 8080 # Container port
      
# Frontend pod to backend pod communication
# Used to communicate inside the Cluster
# DB access inside the cluster
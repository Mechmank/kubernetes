---
apiVersion: v1
kind: Service
metadata:
  name: db-headless
spec:
  ClusterIP: None
  selector:
    app: db
  ports:
    - port: 5432
      targetPort: 5432
      
      
# Used to route the specific request the the same / specific pod always in the cluster even whilw using load balancers